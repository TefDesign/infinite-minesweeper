# Comment mettre à jour le jeu

Le fichier utilisé par le site est `game.min.js` (minifié).
Si vous modifiez `game.js`, vous DEVEZ relancer la commande suivante pour appliquer les changements :

```bash
npm run build
```
